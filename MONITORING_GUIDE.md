# 📊 РУКОВОДСТВО ПО МОНИТОРИНГУ БОТА

**Дата:** 13 ноября 2025  
**Сервер:** 185.70.199.244  
**Статус:** ✅ Исправления применены, бот перезапущен

---

## 🔧 ЧТО БЫЛО ИСПРАВЛЕНО

### Проблема:
Бот не мог анализировать рынок из-за ошибки `TypeError: 'int' object is not iterable` в файле `features.py`.

### Исправления:
1. ✅ `williams_periods` - добавлена проверка типа
2. ✅ `cci_periods` - добавлена проверка типа
3. ✅ `momentum_periods` - добавлена проверка типа
4. ✅ `mfi_periods` - добавлена проверка типа
5. ✅ `ema_trend_periods` - добавлена проверка типа
6. ✅ `cfg.rsi_periods` - добавлена проверка типа
7. ✅ `cfg.ema_periods` - добавлена проверка типа

**Файл:** `/root/bybit_bot/src/bybit_bot/ml/features.py`

---

## 🚀 КОМАНДЫ ДЛЯ МОНИТОРИНГА

### 1. Быстрая проверка статуса
```bash
./check_bot_analysis.sh
```
Показывает:
- Последние циклы анализа
- Анализируемые монеты
- Сигналы и решения
- Ошибки и предупреждения

### 2. Мониторинг в реальном времени
```bash
./monitor_bot_live.sh
```
Показывает логи в реальном времени с цветовой подсветкой:
- 🔵 Голубой - циклы анализа
- 🟢 Зеленый - сигналы (BUY/SELL)
- 🟣 Фиолетовый - монеты (BTC, ETH, SOL, XRP, BNB)
- 🟡 Желтый - предупреждения
- 🔴 Красный - ошибки

### 3. Прямой доступ к логам
```bash
ssh -i ~/.ssh/upcloud_trading_bot root@185.70.199.244 'tail -f /root/bybit_bot/logs/bot.log'
```

### 4. Проверка процесса бота
```bash
ssh -i ~/.ssh/upcloud_trading_bot root@185.70.199.244 'ps aux | grep python | grep run_bot'
```

---

## 📊 ЧТО МОНИТОРИТЬ

### ✅ Успешные циклы анализа
Ищите в логах:
```
TradingOrchestrator._run_cycle_job ... executed successfully
```

### 💰 Анализируемые монеты
Бот анализирует 5 монет:
- BTC/USDT (Bitcoin)
- ETH/USDT (Ethereum)
- SOL/USDT (Solana)
- XRP/USDT (Ripple)
- BNB/USDT (Binance Coin)

### 🎯 Сигналы на открытие позиций
Ищите:
- `signal` - торговые сигналы
- `BUY` / `SELL` - направление сделки
- `confidence` - уверенность (должна быть ≥ 70%)
- `probabilities` - вероятности от ML моделей

### ⚠️ Ошибки
Если видите:
- `TypeError: 'int' object is not iterable` - нужно проверить исправления
- `Failed to process symbol` - проблема с обработкой монеты
- `Failed to rank symbol` - проблема с ранжированием

---

## 🔄 ПЕРЕЗАПУСК БОТА

Если нужно перезапустить бота:
```bash
ssh -i ~/.ssh/upcloud_trading_bot root@185.70.199.244 << 'EOF'
cd /root/bybit_bot
pkill -f 'python.*run_bot.py'
sleep 2
source venv/bin/activate
PYTHONPATH=src:. nohup python scripts/run_bot.py > logs/run.log 2>&1 &
EOF
```

---

## 📈 ОЖИДАЕМОЕ ПОВЕДЕНИЕ

После исправлений бот должен:
1. ✅ Успешно выполнять циклы анализа каждую минуту
2. ✅ Обрабатывать все 5 монет без ошибок
3. ✅ Рассчитывать технические индикаторы
4. ✅ Генерировать сигналы на основе ML моделей
5. ✅ Открывать позиции при уверенности ≥ 70%

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. **Мониторинг:** Запустите `./monitor_bot_live.sh` для наблюдения за работой
2. **Проверка:** Через 5-10 минут проверьте, что ошибки исчезли
3. **Сигналы:** Следите за появлением торговых сигналов
4. **Сделки:** Ожидайте открытия позиций при подходящих условиях

---

**Удачного трейдинга! 🚀**

