# TradeGPT Scalper для Bybit Futures

⚡ **Быстрый скальпер с минимальным профитом +$0.50, автоматическим трейлингом и фиксированным риском**

## 📋 Описание

TradeGPT Scalper - это автоматический торговый бот для Bybit Futures (USDT Perpetual) с упрощенной логикой входа и строгим управлением рисками.

### Ключевые особенности:

- ✅ **Минимальная прибыль**: +$0.50 на сделку
- ✅ **Фиксированный риск**: -$0.15 максимальный убыток
- ✅ **Автоматический трейлинг**: активируется при +$0.35
- ✅ **Плечо**: x20
- ✅ **Размер позиции**: $1 USDT (margin) = $20 USDT (exposure)
- ✅ **Максимум позиций**: 3 одновременно
- ✅ **Торговля**: LONG + SHORT
- ✅ **Монеты**: 145 торговых пар
- ✅ **Упрощенный ML**: Disco57 (только ALLOW/BLOCK)
- ✅ **Без MTF**: только 5m таймфрейм

## 🎯 Логика входа

### Условия для LONG:
1. EMA(9) > EMA(21) - восходящий тренд
2. Текущий объем > средний объем за 10 свечей
3. Momentum(14) > 0 - положительный импульс
4. Disco57 разрешает вход (ALLOW)

### Условия для SHORT:
1. EMA(9) < EMA(21) - нисходящий тренд
2. Текущий объем > средний объем за 10 свечей
3. Momentum(14) < 0 - отрицательный импульс
4. Disco57 разрешает вход (ALLOW)

## 💰 Управление рисками

### Take Profit (TP):
- Автоматический расчет TP для достижения минимум +$0.50
- TP = max(MIN_PROFIT / EXPOSURE, 1.5 * ATR)
- Для $1 x20: TP ≈ 2.5% от entry

### Stop Loss (SL):
- Фиксированный SL = -$0.15
- Для $1 x20: SL = 0.75% от entry
- Устанавливается сразу при входе

### Trailing Stop:
- Активация: при достижении +$0.35 прибыли
- Отступ: 0.15% от максимума прибыли
- Обновление: каждые 5-10 секунд
- После активации фиксированный TP отменяется

### Лимиты:
- **Дневной лимит убытка**: -$5.00 (пауза на 24 часа)
- **Максимум позиций**: 3 одновременно
- **Проверка перед входом**: нет активной позиции по монете

## 🔧 Установка

### 1. Клонирование репозитория
```bash
git clone <repository_url>
cd tradegpt_scalper
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка .env файла
```bash
cp .env.example .env
nano .env
```

Заполните следующие параметры:
```env
BYBIT_API_KEY=your_api_key_here
BYBIT_API_SECRET=your_api_secret_here
BYBIT_TESTNET=true  # false для live торговли

TELEGRAM_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

### 4. Получение API ключей Bybit

1. Зайдите на [Bybit](https://www.bybit.com)
2. Перейдите в API Management
3. Создайте новый API ключ с правами:
   - ✅ Contract Trading
   - ✅ Position
   - ✅ Wallet
4. Скопируйте API Key и Secret в .env

### 5. Настройка Telegram (опционально)

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Узнайте свой Chat ID через [@userinfobot](https://t.me/userinfobot)
4. Добавьте в .env

## 🚀 Запуск

### Тестовый режим (Testnet):
```bash
python tradegpt_scalper.py
```

### Live торговля:
1. Измените в .env: `BYBIT_TESTNET=false`
2. Запустите:
```bash
python tradegpt_scalper.py
```

### Запуск в фоне (Linux/Mac):
```bash
nohup python tradegpt_scalper.py > output.log 2>&1 &
```

### Использование screen:
```bash
screen -S tradegpt
python tradegpt_scalper.py
# Нажмите Ctrl+A, затем D для отсоединения
# screen -r tradegpt для возврата
```

## 📊 Мониторинг

### Логи
Все события записываются в `log.txt`:
```bash
tail -f log.txt
```

### Telegram уведомления
Бот отправляет уведомления о:
- 🚀 Запуске бота
- 🟢 Открытии позиции (OPEN)
- 🔄 Активации трейлинга (TRAILING)
- ✅/❌ Закрытии позиции (CLOSED)
- 📊 Дневной статистике
- ⚠️ Ошибках

### Формат уведомлений

**Открытие:**
```
🟢 OPEN
BTC/USDT:USDT LONG
Вход: $50000.000000
SL: -$0.15
Цель: +$0.50
```

**Трейлинг:**
```
🔄 TRAILING
BTC/USDT:USDT LONG
Прибыль: +$0.35
```

**Закрытие:**
```
✅ CLOSED (TP)
BTC/USDT:USDT LONG
Вход: $50000.000000
Выход: $50250.000000
PnL: +$0.52
Дневной PnL: +$0.52
```

## 🧪 Тестирование модулей

### Тест Bybit API:
```bash
python bybit_api.py
```

### Тест Telegram:
```bash
python telegram_notifier.py
```

### Тест Disco57:
```bash
python disco57_simple.py
```

## 🛡️ Безопасность

### Обязательные проверки:
- ✅ Загрузка активных позиций при старте
- ✅ Запрет повторного входа в ту же монету
- ✅ Блокировка входа при ошибке установки SL/TP
- ✅ Проверка закрытия через API
- ✅ Аварийное закрытие при потере соединения
- ✅ Лимит убытка в день: -$5
- ✅ API ключи в .env (не в коде)
- ✅ Retry механизм (3 попытки при ошибке API)

### Рекомендации:
1. **Начните с testnet** для проверки работы
2. **Используйте небольшой баланс** для первых live сделок
3. **Мониторьте логи** первые несколько часов
4. **Проверяйте позиции** на бирже регулярно
5. **Не изменяйте параметры риска** без понимания последствий

## 📈 Статистика и обучение

### Disco57 Learning
Бот автоматически обучается на каждой закрытой сделке:
- Прибыльные сделки → увеличивают веса признаков
- Убыточные сделки → уменьшают веса признаков
- Модель сохраняется каждые 10 сделок в `disco57_model.pkl`

### Просмотр статистики:
```python
from disco57_simple import Disco57Simple
disco = Disco57Simple()
stats = disco.get_stats()
print(f"Сделок: {stats['total_trades']}")
print(f"Win Rate: {stats['win_rate']:.1f}%")
print(f"Веса: {stats['weights']}")
```

## 🔧 Настройка параметров

Основные параметры находятся в `tradegpt_scalper.py`:

```python
POSITION_SIZE = 1.0  # $1 USDT margin
LEVERAGE = 20  # x20
MAX_POSITIONS = 3
MIN_PROFIT_USD = 0.50  # Минимальная прибыль
MAX_LOSS_USD = 0.15  # Максимальный убыток
DAILY_MAX_LOSS_USD = 5.0  # Дневной лимит
TRAILING_ACTIVATION_USD = 0.35  # Активация трейлинга
TRAILING_DISTANCE_PCT = 0.0015  # 0.15% отступ
SCAN_INTERVAL_SEC = 300  # 5 минут
```

## ❓ FAQ

### Q: Почему бот не открывает позиции?
A: Проверьте:
- Логи в `log.txt` для причин блокировки
- Достаточный баланс на аккаунте
- Правильные API ключи
- Disco57 может блокировать входы (проверьте веса)

### Q: Как изменить список монет?
A: Отредактируйте массив `TRADING_SYMBOLS` в `tradegpt_scalper.py`

### Q: Можно ли увеличить размер позиции?
A: Да, измените `POSITION_SIZE` и `LEVERAGE`, но помните о рисках

### Q: Как остановить бота?
A: Нажмите `Ctrl+C` или `kill <PID>`

### Q: Бот закроет позиции при остановке?
A: Нет, позиции останутся открытыми. Закройте их вручную или перезапустите бота

## 📝 Структура проекта

```
tradegpt_scalper/
├── tradegpt_scalper.py    # Основной файл бота
├── bybit_api.py           # Обертка для Bybit API
├── disco57_simple.py      # Упрощенный ML модуль
├── telegram_notifier.py   # Telegram уведомления
├── requirements.txt       # Зависимости
├── .env.example          # Пример конфигурации
├── .env                  # Ваша конфигурация (не коммитить!)
├── log.txt              # Логи бота
├── disco57_model.pkl    # Обученная модель (создается автоматически)
└── README.md            # Этот файл
```

## 🚨 Disclaimer

**ВНИМАНИЕ:** Торговля криптовалютами с использованием плеча сопряжена с высоким риском. Вы можете потерять весь свой капитал. Этот бот предоставляется "как есть" без каких-либо гарантий. Автор не несет ответственности за ваши убытки.

**Рекомендации:**
- Используйте только те средства, которые можете позволить себе потерять
- Начните с testnet
- Тщательно тестируйте на малых суммах
- Регулярно проверяйте работу бота
- Не оставляйте бота без присмотра надолго

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте логи в `log.txt`
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность API ключей
4. Попробуйте запустить тесты модулей

## 📄 Лицензия

MIT License

---

**Удачной торговли! 🚀**
